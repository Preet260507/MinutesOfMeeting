@model MOM_Project.Models.MeetingVenue
@{ ViewData["Title"] = "Delete Venue"; }

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card border-0 shadow-lg text-center p-4">
                <div class="mb-3">
                    <div class="rounded-circle bg-danger bg-opacity-10 d-inline-flex p-3">
                        <i class="bi bi-trash-fill text-danger fs-1"></i>
                    </div>
                </div>
                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="alert alert-danger shadow-sm mb-4">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewBag.ErrorMessage
                    </div>
                }
                <h4 class="fw-bold text-dark">Delete Venue?</h4>
                <p class="text-muted">Are you sure you want to remove <strong>@Model.MeetingVenueName</strong>?</p>
                
                <form asp-action="Delete" class="mt-3">
                    <input type="hidden" asp-for="MeetingVenueID" />
                    <a asp-action="Index" class="btn btn-light border px-4">Cancel</a>
                    <button type="submit" class="btn btn-danger px-4 ms-2">Delete Venue</button>
                </form>
            </div>
        </div>
    </div>
</div>